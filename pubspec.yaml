name: bd_l10n
description: Tools that help manage localization for a dart/flutter application.
version: 1.4.1
homepage: https://github.com/bitsydarel/bd_l10n
repository: https://github.com/bitsydarel/bd_l10n
issue_tracker: https://github.com/bitsydarel/bd_l10n/issues

environment:
  sdk: '>=2.18.6 <4.0.0'

dependencies:
  args: ">=2.0.0 <3.0.0"
  build: ">=2.0.0 <3.0.0"
  build_config: ">=1.0.0 <2.0.0"
  io: ">=1.0.0 <2.0.0"
  json_annotation: ">=4.8.0 <5.0.0"
  meta: ">=1.3.0 <3.0.0"
  path: ">=1.8.1 <2.0.0"
  watcher: ">=1.1.0 <2.0.0"
  yaml: ">=3.0.0 <4.0.0"

dev_dependencies:
  build_runner: ">=2.0.0"
  flutter_code_style: ^2.2.0 # project code style.
  json_serializable: ">=6.6.0" # create json conversion utility.
  test: ">=1.16.6"

executables:
  bd_l10n:
