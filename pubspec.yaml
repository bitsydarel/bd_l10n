name: bd_l10n
description: Tools that help manage localization for a dart/flutter application.
version: 1.5.0
homepage: https://github.com/bitsydarel/bd_l10n
repository: https://github.com/bitsydarel/bd_l10n
issue_tracker: https://github.com/bitsydarel/bd_l10n/issues

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  args: ">=2.6.0 <3.0.0"
  build: ">=2.5.0 <3.0.0"
  build_config: ">=1.1.2 <2.0.0"
  io: ">=1.0.5 <2.0.0"
  json_annotation: ">=4.9.0 <5.0.0"
  meta: ">=1.16.0 <2.0.0"
  path: ">=1.9.0 <2.0.0"
  watcher: ">=1.1.0 <2.0.0"
  yaml: ">=3.1.3 <4.0.0"

dev_dependencies:
  build_runner: ">=2.4.7"
  flutter_code_style: ^2.2.0 # project code style.
  json_serializable: ">=6.7.0" # create json conversion utility.
  test: ">=1.25.6"

executables:
  bd_l10n:
